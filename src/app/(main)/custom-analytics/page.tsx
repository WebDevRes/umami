import CustomAnalyticsPage from './CustomAnalyticsPage';
import { Metadata } from 'next';

export default function Page() {
  return <CustomAnalyticsPage />;
}

export const metadata: Metadata = {
  title: 'Custom Analytics',
};
